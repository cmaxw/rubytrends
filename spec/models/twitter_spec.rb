require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe TwitterSearch do
  before(:each) do
    @search = TwitterSearch.new("ruby")
  end

  it "creates urls from the tweets returned by the search"
    
  end
end
